[project]
name = 'Electric-Vehicle-Charging-Tokens'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.Electric-Vehicle-Charging-Tokens]
path = 'contracts/Electric-Vehicle-Charging-Tokens.clar'
clarity_version = 3
epoch = 3.1
[contracts.charging-session-registry]
path = 'contracts/charging-session-registry.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
